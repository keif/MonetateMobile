/**
 * @format
 */

import {AppRegistry} from 'react-native';
import MonetateMobile from './src/index';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => MonetateMobile);
